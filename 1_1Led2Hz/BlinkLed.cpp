#include <iostream>
#include <string>

using namespace std;
int main()
{
   ?? porta;
   ?? ddra;
   ?? salida;

   DDRA =1;
   while (1)
   {
      PORTA =1;
      sleep(0.5);
      PORTA =0;
      sleep(0.5);
      salida =false;
      if (salida==true)) break;
   }

   return 0;
}
